{
  "name": "@aa/timber",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "**/*.css"],
  "sideEffects": ["**/*.css"],
  "peerDependencies": {
    "react": ">=18",
    "react-dom": ">=18",
    "@remix-run/react": ">=2",
    "@mantine/core": ">=8",
    "@mantine/hooks": ">=8",
    "@tabler/icons-react": ">=3"
  },
  "devDependencies": {
    "tsup": "^8.3.0",
    "typescript": "^5.7.2"
  },
  "scripts": {
  "build": "tsup src/index.ts --dts --format esm,cjs --sourcemap --clean && mkdir -p dist/admin && cp src/admin/*.css dist/admin/ || true"
  }
}
